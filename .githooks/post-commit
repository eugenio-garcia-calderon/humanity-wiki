#!/bin/sh
# Recordatorio, no bloqueo: soltar es la segunda mitad de commitear.
raiz=$(git rev-parse --show-toplevel)
command -v node >/dev/null 2>&1 || exit 0
[ -f "$raiz/scripts/equipo.mjs" ] || exit 0
node "$raiz/scripts/equipo.mjs" recordar 2>/dev/null
exit 0
