/// <reference types="vite/client" />
// Los tipos del cliente de Vite (2026-08-22, Programador 3). El proyecto no los
// tenía porque nadie había usado `import.meta.env` en `src/` todavía; sin esta
// línea, TypeScript no sabe que existe. Fichero estándar de cualquier proyecto
// Vite: no cambia comportamiento, solo hace que el compilador conozca la API.
